const fs=require("fs"),{catalogFile}=require("./config");const c=JSON.parse(fs.readFileSync(catalogFile,"utf8"));let ok=true;for(let i=0;i<c.items.length;i++){const x=c.items[i];if(!/^tt\d+$/.test(x.id))ok=false;if(!/^\d{4}-\d{2}-\d{2}$/.test(x.greekTheatricalDate))ok=false;if(i&&c.items[i-1].greekTheatricalDate<x.greekTheatricalDate)ok=false}console.log(ok?`OK: ${c.items.length} items`:"FAILED");process.exit(ok?0:1);
